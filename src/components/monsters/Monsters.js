import React from 'react';
import Spinner from '../layout/Spinner';

const Monsters = ({ loading }) => {
    if(loading) {
        return <Spinner />
    } else {
        return (
            
        )
    }
    return (
        <div>
            
        </div>
    )
}

export default Monsters
